# Handoff 2026-07-27（第 2 次）

## 結果

跑到：M14；M0–M10、M12–M14 全綠並已 commit，M11 的 CPU／Colab foundation 已 commit。

狀態：**停在 M11 的 real-model smoke**

原因：

- pinned SDXL base weights 超過無人值守 2 GB 下載界線，需要使用者明確同意；
- 3_FormosaNLU 正在執行正式生成，跨專案 GPU 鎖要求 DefectForge 不得配置 GPU 顯存；
- M11 必須取得 real-model fresh／resume／reload 與 peak VRAM 證據後才能勾選，
  靜態測試與 dry-run 不得冒充完成。

## 完成的里程碑

| M | 標題 | commit | 驗證 |
|---|---|---|---|
| M1 | uv／CUDA 環境 | `b9c8578` | 全綠 |
| M2 | VisA 下載與 checksum | `75bc7e1` | 全綠 |
| M3 | 官方 split | `c8c1749` | 全綠 |
| M4 | pHash split 與 test guard | `50615c4` | 全綠 |
| M5 | deterministic few-shot | `b2a607a` | 全綠 |
| M6 | DINOv2 defect clustering | `a063d72` | 全綠 |
| M7 | copy-paste Stage A | `f347a0f` | 全綠 |
| M8 | procedural Stage A | `ae598b8` | 全綠 |
| M9 | SDG legal placement | `aa5c7e2` | 全綠 |
| M10 | SD2 inpainting LoRA | `dbd9ca0` | 全綠 |
| M12 | SD2 original／searched generation | `73aba3d` | 全綠 |
| M13 | 六道 deterministic filtering | `ee80470` | 3,000 筆重驗通過 |
| M14 | mNN／KID 品質評估 | `84a2158` | 44 rows、4 sanity checks |
| M11 foundation | SDXL trainer／notebook／bundle | `08c020d` | CPU 部分全綠；real smoke 待辦 |

## 停下來的細節

- M11 model lock：
  `diffusers/stable-diffusion-xl-1.0-inpainting-0.1@115134f363124c53c7d878647567d04daf26e41e`。
- pcb1／capsules dry-run 分別為 23／12 component samples、每物件 10 張來源圖；
  frozen manifest、selection、defect-types 與四個 model-file SHA256 全部相符。
- SDXL conditioning 已涵蓋雙 tokenizer、雙 TrainableTokens adapter、兩組 penultimate
  hidden states、pooled text embedding 與 1024 time IDs。
- 完整 CPU regression：67 tests passed；Ruff、Notebook validator、skill validator、
  M10 bundle validator、M13 verifier、M14 verifier全部通過。
- M10 相容性另以兩份既有正式 report 驗證；修改後 SD2 run signatures 逐字相同。
- M13 verifier：3,000 records、1,770 accepted、1,230 rejected、9,540 hardlinks，
  summary SHA256 `831ec0936a7e3068c30f46f5fac1240634ae0de23aa8dc3e8ad9db87dd88a8a0`。
- M14 verifier：44 rows、4 sanity checks、0 blocklist hits，
  summary SHA256 `906f768b65654e96d2e41c724b019d613d48760934f395017f0cf24f2a626a07`。

我沒有動的東西：

- 沒有啟動 CUDA、PyTorch GPU、Ollama 或其他配置 GPU 顯存的程序；
- 沒有終止、暫停或修改 3_FormosaNLU；
- 沒有下載 SDXL 權重、啟動 Colab或花費 compute units；
- 沒有建立 GitHub repo、設定 remote、push、PR 或對外上傳；
- 沒有讀出或寫入任何 secret，也沒有修改 frozen split／blocklist／source checksum。

## 需要你決定

1. 等 3_FormosaNLU 發出 GPU 解除通知後，是否允許下載 pinned SDXL 權重（超過 2 GB）
   並執行本機 1-step fresh／resume／reload smoke？**建議選這個**，先在本機把 M11
   真實路徑驗完，再使用 Colab。
2. 是否建立 GitHub repo；建立後再由你指定 remote／可見性，我才會連接或 push。

## 產出物

- `notebooks/01_train_inpaint_lora_sdxl.ipynb`
- `configs/lora_sdxl.yaml`
- `D:\sdg-data\01-defectforge\colab\m11\defectforge_source.zip`
- `D:\sdg-data\01-defectforge\colab\m11\m11_sdxl_inputs.zip`
- `D:\sdg-data\01-defectforge\colab\m11\m11_colab_bundle.json`
- `reports/figures/filter_accepted.png`
- `reports/figures/filter_rejected.png`
- `reports/figures/generation_quality_nn.png`
- `reports/figures/generation_quality_real_vs_generated.png`

Colab bundle 的 bytes、file counts 與 SHA256 以同目錄
`m11_colab_bundle.json` 為唯一準據。舊 bundle 均移入同目錄的 `archive-*` 備份，
未刪除，可回復。

## 花費

Colab 0 compute units；金錢 0。跨專案 GPU 鎖生效後，DefectForge GPU 使用量為 0。
本次無人值守總執行時間約 9.5 小時，未超過 10 小時上限。
