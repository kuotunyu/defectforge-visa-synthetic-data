# Handoff 2026-07-27

## 結果

跑到：M1（M0–M1 全綠並已 commit）

狀態：**停在 M2**

原因：M2 是 🙋 里程碑；VisA 下載為 1,929,840,640 bytes，專案規則要求第一次大量
寫入磁碟前取得使用者明確同意。

## 完成的里程碑

| M | 標題 | commit | 驗證 | 耗時 |
|---|---|---|---|---|
| M1 | uv 環境與鎖版 | `chore(M1): lock CUDA environment and verify runtime` | 全綠 | 約 20 min |

## 停下來的細節

- 不是驗證失敗；是預定的人工作業邊界。
- 已完成：Python 3.12.13、torch 2.13.0+cu130、CUDA 13.0、RTX 4090、
  175-package lockfile、完整核心 import smoke test。
- 安裝第一次在 `noise==1.2.2` 失敗；確認它只有 2015 年的 source distribution 與
  CPython 3.4 Windows wheel後，移除該非必要 C extension，第二次安裝成功。
- 我沒有下載 VisA、沒有讀取或輸出任何 secret、沒有啟動 Colab、沒有建立 GitHub repo、
  沒有設定 remote、沒有 push，也沒有動另外兩個專案。

## 需要你決定

1. 是否授權 M2 下載 VisA 1.80 GB 到 `D:\sdg-data\01-defectforge\raw`？
   我傾向授權；D: preflight 尚有約 1.73 TiB，空間充足。

## 產出物

- `uv.lock`
- `.venv/`（git ignored）
- `docs/environment.md`
- `docs/troubleshooting.md`
- `docs/worklog.md`

## 花費

本機 GPU 訓練時數 0；Colab 0 compute units；金錢 0。只有 CUDA 初始化與 import smoke
test，未占用長時間 GPU 工作。
